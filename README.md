# Edison

Logiciel de montage vidéo en Python (modules R5.A.05 / R5.A.06).

## Installation

```bash
python -m venv .venv
source .venv/bin/activate        # Windows : .venv\Scripts\activate
pip install -e ".[dev]"
```

## Lancer

```bash
python -m edison
```

## Tests

```bash
pytest
```
